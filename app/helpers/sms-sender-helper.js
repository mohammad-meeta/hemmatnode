'use strict';

const ghasedak = require('ghasedak');

/**
 * dep cat controller
 */
function SMSSenderHelper() {}
module.exports = SMSSenderHelper;

/**
 * insert file data  
 */
SMSSenderHelper.sendSms = function sendSms(data) {
    console.log(data);
};