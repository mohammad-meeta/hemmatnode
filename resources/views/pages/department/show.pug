extends ../../layout/app.pug

block title
    | title

block content
    show-department(ref="departmentShow", show-load-url=route("api.department.show", { department: '$department$'}),
                 department-id=departmentId)

block scripts
    script(defer src=mix("js/pages/department/show/index.js"))
