extends ../../layout/app.pug

block title
    | دعوتنامه

block content
    .container
        invite-sessions(list-url=route('api.invitesession', { page: '$page$', size: '$pageSize$', group: departmentId }),
            departments-url=route('api.department', { page: 1, size: 1000 }),
            register-url=route('invitesession.store', { department: departmentId }),
            edit-url=route('invitesession.update', { id :'$id$'}),
            users-url=route('api.user', { page: 1, size: 1000 }),
            department-id=departmentId
        )

block scripts
    script(defer, src=mix('js/pages/invite-session/index/index.js'))


