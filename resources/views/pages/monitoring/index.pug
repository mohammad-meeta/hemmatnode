extends ../../layout/app.pug

block title
    | پیوست سلامت

block content
    .container
        monitorings(
            list-url=route('api.index', { page: '$page$', size: '$pageSize$' }),
            register-url=route('index.store'}),
            edit-url=route('index.update', { id :'$id$'}),
        )

block scripts
    script(defer, src=mix('js/pages/monitoring/index/index.js'))


