extends ../../layout/app.pug

block title
    | برنامه

block content
    .container
        programs(
            list-url=route('api.program', { page: '$page$', size: '$pageSize$', group: departmentId }),
            show-pragram-year-url=route("api.program.year", { group: departmentId, year:year,page:'$page$',size:'$pageSize$'}),
            register-url=route('program.store', { department: departmentId }),
            edit-url=route('program.update', { id :'$id$'}),
            department-id=departmentId,
            year=year
        )

block scripts
    script(defer, src=mix('js/pages/program/index/index.js'))


