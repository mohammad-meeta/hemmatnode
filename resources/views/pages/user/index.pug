extends ../../layout/app.pug

block title
    | کاربران

block heading
    section.hero
        .container.page-header
            .title
                h1 کاربران

block content
    .container.has-text-centered
        user-list(ref="userList" list-url=route('api.user'))

block scripts
    script.
        document.data = {
            urls: {
                usersList: '#{route("user.index")}'
            }
        };
    script(defer, src=mix('js/pages/user/index/index.js'))

